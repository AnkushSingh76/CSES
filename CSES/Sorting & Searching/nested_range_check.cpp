#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<vector<int>>temp;
    for(int i=0;i<n;i++){
        int a,b;
        cin>>a>>b;
        if(temp.empty()){
            temp.push_back({a,b});
        }
        else{
            
        }
    }
}